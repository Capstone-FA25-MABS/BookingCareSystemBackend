# ?? Test S3-Only Mode - Communication Service

Sau khi configuration ?ã ???c c?p nh?t, t?t c? uploads s? ???c route t?i AWS S3.

## ?? **Test Cases**

### **1. Test Image Upload (Should go to S3 now)**
```bash
curl -X POST "http://localhost:6009/api/enhanced-fileupload/smart-upload" \
  -F "file=@test-image.jpg" \
  -F "userId=user-123" \
  -F "messageType=Image"
```

**Expected Response:**
```json
{
  "success": true,
  "data": {
    "result": {
      "url": "https://d24em9p7s2uixh.cloudfront.net/communication/user-123/image/test-image-unique.jpg",
      "fileName": "test-image-unique.jpg",
      "size": 1024000,
      "mimeType": "image/jpeg",
      "thumbnailUrl": "https://d24em9p7s2uixh.cloudfront.net/communication/user-123/image/test-image-unique.jpg"
    },
    "provider": "AWS S3 + CloudFront",
    "routing": "Auto-routed based on MessageType and content"
  },
  "message": "Smart upload thành công qua AWS S3 + CloudFront!"
}
```

### **2. Test Video Upload (Should go to S3 now)**
```bash
curl -X POST "http://localhost:6009/api/enhanced-fileupload/smart-upload" \
  -F "file=@test-video.mp4" \
  -F "userId=user-123" \
  -F "messageType=Video"
```

### **3. Test Document Upload (Already S3)**
```bash
curl -X POST "http://localhost:6009/api/enhanced-fileupload/smart-upload" \
  -F "file=@document.pdf" \
  -F "userId=user-123" \
  -F "messageType=File"
```

## ?? **Log Verification**

Check application logs for these messages:
```
Force S3 mode enabled - routing all uploads to S3
Routing to AWS S3 for Image with content type image/jpeg
Routing to AWS S3 for Video with content type video/mp4
```

## ?? **S3 Structure Verification**

After successful uploads, check your S3 bucket structure:
```
mabs-capstone-fa25-booking-care-s3-bucket/
??? communication/
?   ??? user-123/
?   ?   ??? image/
?   ?   ?   ??? test-image-unique.jpg     ? New! (was in Cloudinary)
?   ?   ??? video/
?   ?   ?   ??? test-video-unique.mp4     ? New! (was in Cloudinary)
?   ?   ??? file/
?   ?   ?   ??? document-unique.pdf       ? Same as before
?   ?   ??? audio/
?   ?   ??? voicenote/
```

## ?? **Key Differences**

| Aspect | Before (Hybrid) | After (S3-Only) |
|--------|----------------|-----------------|
| **Image URLs** | `res.cloudinary.com/...` | `d24em9p7s2uixh.cloudfront.net/...` |
| **Video URLs** | `res.cloudinary.com/...` | `d24em9p7s2uixh.cloudfront.net/...` |
| **Thumbnails** | Auto-generated by Cloudinary | Same as original URL |
| **Processing** | Cloudinary optimization | Raw file storage |

## ?? **Notes**

1. **Thumbnails:** Current implementation returns original URL as thumbnail
2. **Processing:** No automatic image/video optimization (can be added via Lambda)
3. **Performance:** CloudFront CDN still provides fast delivery
4. **Cost:** Potentially lower costs with unified S3 storage

## ?? **Next Steps**

After confirming S3-only mode works correctly:

1. **Monitor Performance:** Check upload/download speeds
2. **Review Costs:** Compare AWS vs Cloudinary costs
3. **Plan Lambda:** For thumbnail generation if needed
4. **Remove Dependencies:** Plan Cloudinary package removal